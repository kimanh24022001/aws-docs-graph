[build]
builder = "DOCKERFILE"
dockerfilePath = "agent-service/Dockerfile.railway"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/docs"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
